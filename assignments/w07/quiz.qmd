---
title: "Quiz w07 — Minggu 07"
format: html
execute:
  echo: true
  warning: false
  message: false
---

## Cara mengerjakan

- Jawab **hanya** dengan mengisi fungsi `q01()` s.d. `q12()` di `submissions/w07/answers.py`.
- Kamu boleh memakai Python untuk menghitung, misalnya lewat `submissions/w07/laporan.qmd`.
- Jangan ubah nama fungsi.

### Format jawaban

- **T/F**: kembalikan `True` (Benar) atau `False` (Salah).
- **MC**: kembalikan huruf `"A"`, `"B"`, `"C"`, atau `"D"`.
- **Numeric**: kembalikan bilangan `int` atau `float` (pakai titik `.` untuk desimal).

---

## Q01 (T/F)

Total luas di bawah kurva fungsi padat probabilitas (PDF) selalu sama dengan 1.

## Q02 (T/F)

Distribusi Normal Standar memiliki mean 0 dan variansi 1.

## Q03 (T/F)

Pada distribusi kontinu, $P(X \leq x)$ selalu sama dengan $P(X < x)$.

## Q04 (MC)

Nilai $Z$ yang bersesuaian dengan nilai $X$ pada distribusi $N(\mu, \sigma^2)$ adalah:

- **A)** $(X-\mu)/\sigma$
- **B)** $(X+\mu)/\sigma$
- **C)** $\sigma/(X-\mu)$
- **D)** $X-\mu$

## Q05 (MC)

Distribusi mana yang sering digunakan untuk memodelkan waktu antar kejadian dalam proses Poisson?

- **A)** Binomial.
- **B)** Normal.
- **C)** Eksponensial.
- **D)** Uniform.

## Q06 (MC)

Sekitar 95% data dalam distribusi Normal berada dalam rentang:

- **A)** $\mu \pm \sigma$
- **B)** $\mu \pm 2\sigma$
- **C)** $\mu \pm 3\sigma$
- **D)** $0 \pm 1$

## Q07 (MC)

Jika $\lambda = 0,5$ pada distribusi Eksponensial, maka nilai harapannya adalah:

- **A)** 0,5
- **B)** 2,0
- **C)** 1,0
- **D)** 0,25

## Q08 (Numeric)

Jika $Z \sim N(0,1)$, berapakah $P(Z \leq 0)$?

## Q09 (Numeric)

Sebuah komponen memiliki rata-rata waktu hidup 100 jam (Eksponensial). Berapakah parameter $\lambda$-nya?

## Q10 (Numeric)

Nilai $Z$ untuk probabilitas kumulatif 0,975 adalah sekitar... (Gunakan 2 desimal)

## Q11 (Numeric)

Jika $X \sim N(10, 4)$, berapakah nilai skor-Z untuk $X = 12$?

## Q12 (Numeric)

Pada distribusi Normal Standar, berapakah nilai $P(Z > 3)$ mendekati? (Gunakan 4 desimal)
